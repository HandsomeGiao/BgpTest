[project]
name = "topo-generator"
version = "0.1.0"
description = "PyQt topology editor for the BGP TopoSimulator JSON schema"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.6"
]

[project.scripts]
topogenerator = "topogenerator.main:main"

